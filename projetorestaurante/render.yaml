services:
  - type: web
    name: projeto-restaurante
    env: python
    buildCommand: "./build.sh"
    
    # --- CORREÇÃO FINAL ---
    # --chdir diz ao Gunicorn para entrar nesta pasta antes de rodar
    startCommand: "gunicorn --chdir projetorestaurante/projetorestaurante restaurante.wsgi:application"
    
    plan: free

    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: restaurante-db
          property: connectionString
      - key: SECRET_KEY
        generateValue: true
      - key: WEB_CONCURRENCY
        value: 4

databases:
  - name: restaurante-db
    plan: free